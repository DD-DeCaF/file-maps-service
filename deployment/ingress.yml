apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: filemapsservice
spec:
  tls:
  - hosts:
    - api.dd-decaf.eu
    - api-staging.dd-decaf.eu
    secretName: decaf-tls
  rules:
  - host: api.dd-decaf.eu
    http:
      paths:
      - path: /filemapsservice
        backend:
          serviceName: filemapsservice-production
          servicePort: 80
  - host: api-staging.dd-decaf.eu
    http:
      paths:
      - path: /filemapsservice
        backend:
          serviceName: filemapsservice-staging
          servicePort: 80
