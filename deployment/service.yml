kind: Service
apiVersion: v1
metadata:
  name: maps-production
spec:
  selector:
    app: maps
    env: production
  ports:
  - name: web
    protocol: TCP
    port: 80
    targetPort: 8000

---

kind: Service
apiVersion: v1
metadata:
  name: maps-staging
spec:
  selector:
    app: maps
    env: staging
  ports:
  - name: web
    protocol: TCP
    port: 80
    targetPort: 8000
