apiVersion: apps/v1
kind: Deployment
metadata:
  name: filemapsservice-production
spec:
  selector:
    matchLabels:
      app: filemapsservice
      env: production
  replicas: 1
  template:
    metadata:
      labels:
        app: filemapsservice
        env: production
    spec:
      containers:
      - name: web
        image: gcr.io/dd-decaf-cfbf6/filemapsservice:master
        imagePullPolicy: Always
        ports:
        - containerPort: 8000
        env:
        - name: ENVIRONMENT
          value: production
        - name: SCRIPT_NAME
          value: /filemapsservice
        - name: FLASK_APP
          value: src/filemapsservice/wsgi.py
        - name: ALLOWED_ORIGINS
          value: https://app.dd-decaf.eu,http://localhost:4200
        - name: SENTRY_DSN
          valueFrom:
            secretKeyRef:
              name: filemapsservice-production
              key: SENTRY_DSN
        command: ["gunicorn", "-c", "gunicorn.py", "filemapsservice.wsgi:app"]
        resources:
          requests:
            cpu: "10m"
          limits:
            cpu: "100m"

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: filemapsservice-staging
spec:
  selector:
    matchLabels:
      app: filemapsservice
      env: staging
  replicas: 1
  template:
    metadata:
      labels:
        app: filemapsservice
        env: staging
    spec:
      containers:
      - name: web
        image: gcr.io/dd-decaf-cfbf6/filemapsservice:devel
        imagePullPolicy: Always
        ports:
        - containerPort: 8000
        env:
        - name: ENVIRONMENT
          value: staging
        - name: SCRIPT_NAME
          value: /filemapsservice
        - name: FLASK_APP
          value: src/filemapsservice/wsgi.py
        - name: ALLOWED_ORIGINS
          value: https://staging.dd-decaf.eu,http://localhost:4200
        - name: SENTRY_DSN
          valueFrom:
            secretKeyRef:
              name: filemapsservice-staging
              key: SENTRY_DSN
        command: ["gunicorn", "-c", "gunicorn.py", "filemapsservice.wsgi:app"]
        resources:
          requests:
            cpu: "5m"
          limits:
            cpu: "100m"
